# AlpineBank synthetic fixtures

AlpineBank is fictional and exists only for the FinBridge candidate demonstration.

PASS 0 freezes the scenario manifest but intentionally does not yet materialize all 1,024 transaction rows. The deterministic dataset generator and golden-path batch will be implemented in PASS 1.
