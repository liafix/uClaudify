# FinBridge Cloud — PASS 1 Evidence

Date: 2026-09-05

## Scope

Implemented approved golden vertical slice only:

`Legacy input -> Python processing -> QUALITY BLOCKED -> anomalies -> quarantine -> reconciliation -> idempotent replay -> READY FOR ANALYTICS`

No PASS 2+ scope was intentionally started.

## Executable domain evidence

The deterministic Python pipeline produces:

- input records: **1,024**
- accepted candidates: **1,019**
- duplicate anomalies: **3**
- invalid-currency anomalies: **2**
- quarantined after explicit operator action: **5**
- unaccounted after classification/quarantine: **0**
- credits from accepted records: **€125,400.00**
- debits from accepted records: **€98,700.00**
- expected closing balance: **€1,026,700.00**
- calculated closing balance: **€1,026,700.00**
- reconciliation difference: **€0.00**
- replay new accepted records: **0**
- replay duplicate imports: **0**
- replay additional quarantine rows: **0**
- final state: **READY_FOR_ANALYTICS**

## Local validation results

`./scripts/pass1_gate.sh` => **GREEN**

The gate contains:

- PASS 0 frozen-contract regression validation — PASS
- PASS 1 executable golden-path validation — PASS
- Python unittest suite — **14/14 PASS**
- Java PASS 0 boundary compile/contract smoke — PASS
- frontend strict TypeScript fallback declaration check — PASS
- frontend Python-evidence contract check — PASS
- secret hygiene / synthetic-only safeguards — PASS
- `git diff --check` — PASS
- Python `compileall` — PASS

## Frontend evidence boundary

`apps/demo-web/data/golden-path.json` is regenerated by executing the Python domain engine. `scripts/pass1_validate.py` fails if the committed frontend evidence differs from a fresh runtime execution. The browser is presentation-only.

## Production Next.js build status

A local `npm install` was attempted in this execution environment, but access to the npm registry timed out. Therefore **a local Next.js production build is not claimed as PASS evidence**.

The repository includes `.github/workflows/pass1.yml`, which installs the declared frontend dependencies, reruns the PASS 1 gate using the real React/Next type packages, and then runs `npm run build` in an internet-enabled GitHub Actions environment. Dependency lock/release hardening remains a later-pass concern; no cloud CI result is fabricated here.

## Deferred by approved plan

- Azure/Azurite storage boundary — PASS 3
- real Java legacy mapper/exporter — PASS 4
- MCP server — PASS 5
- Terraform/Azure resource implementation — PASS 6
- final candidate UX/mapping — PASS 7+

## Verdict

**PASS 1 local implementation gate: GREEN.**

**External frontend dependency install/Next production-build evidence: PENDING cloud/network-capable validation, explicitly not claimed.**
