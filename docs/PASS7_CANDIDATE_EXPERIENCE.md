# PASS 7 — Candidate Experience + uCloudify Mapping

Five candidate routes present the already-frozen evidence without moving domain ownership into the UI:

- `/` Pipeline — interactive golden path.
- `/quality` — DQ-01/DQ-02, accountability and exact reconciliation.
- `/operations` — six read-only MCP tools and visible `evidence_used` provenance.
- `/architecture` — Java/Python/Azure-Azurite/Terraform/MCP/finance boundaries.
- `/candidate` — uCloudify role signals mapped to executable evidence and explicit non-claims.

The UI is presentation-only. Python still owns all validity/readiness decisions. Java remains a legacy transport adapter. Terraform remains infrastructure-only. MCP remains read-only. No real uCloudify client data or proprietary architecture is represented.
